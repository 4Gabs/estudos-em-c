#include <stdio.h>

int main(){

    char letra;
    int nro;
    float n;
    double d;

    scanf("%c", &letra);
    scanf("%d", &nro);
    scanf("%f", &n);
    scanf("%f", &d);

    scanf("tipos de entrada",variaveis);

    return 0;
}